set(YEWPAR_BUILD_ENUMERATION_APPS_NQUEENS "ON" CACHE BOOL "Build NQueens Enumeration")

if (YEWPAR_BUILD_ENUMERATION_APPS_NQUEENS)
add_hpx_executable(nqueens
  SOURCES main.cpp
  DEPENDENCIES YewPar)

endif (YEWPAR_BUILD_ENUMERATION_APPS_NQUEENS)
